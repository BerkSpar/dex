import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:dex/app/modules/home/pages/pokemon/pokemon_page.dart';

void main() {
  testWidgets('PokemonPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(PokemonPage(title: 'Pokemon')));
    //  final titleFinder = find.text('Pokemon');
    //  expect(titleFinder, findsOneWidget);
  });
}
